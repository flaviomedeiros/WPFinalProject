<UserControl x:Class="WP_Final_Project.View.ItemReceitaBusca"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    Height="70" >

    <Grid x:Name="LayoutRoot" Tap="LayoutRoot_Tap">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60*"/>
            <ColumnDefinition Width="40*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" x:Name="Compatibilidade" Text="100%" Margin="10 0 0 0" VerticalAlignment="Center" FontSize="25"/>
        <RichTextBox Grid.Column="1">
            <Paragraph>
                <Run x:Name="NomeReceita" FontSize="25"/>
            </Paragraph>
        </RichTextBox>

    </Grid>
</UserControl>