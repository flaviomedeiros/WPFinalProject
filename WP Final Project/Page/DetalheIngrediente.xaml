<phone:PhoneApplicationPage
    x:Class="WP_Final_Project.Page.DetalheIngrediente"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="55*"/>
            <RowDefinition Height="40*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Lista de ingredientes da receita" Margin="12, 0, 0, 0" FontSize="30"/>

        <ScrollViewer  Grid.Row="1">
            <TextBlock  x:Name="DescritivoIngrediente" Margin="12, 0, 0, 0" FontSize="30"/>
        </ScrollViewer>
        
        <Grid Grid.Row="2" >
            <ScrollViewer  >
                <TextBlock x:Name="ListaDeSupermercados" />
            </ScrollViewer>
        </Grid>
    </Grid>


</phone:PhoneApplicationPage>